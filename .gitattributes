# Normalize line endings to LF in the repo. The GitHub Actions runner is
# Linux, and check.yml contains a shell script that must stay LF.
* text=auto eol=lf

# Binary/generated state written by the bot — let Git handle as text but
# don't try to diff noisily.
state.json text eol=lf
